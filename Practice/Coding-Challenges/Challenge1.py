# Challenge 1: Area of a Rectangle
# Write a Python script that calculates the area of a rectangle. 
# Prompt the user to enter the length and width of the rectangle, then calculate and print the area.

length = int(input("Enter the Length in cm: "))
breadth = int(input("Enter the Breadth in cm: "))

print("The Area of Rectangle is:", length * breadth)
